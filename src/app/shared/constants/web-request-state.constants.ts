import {
  LoadingState,
  NotAskedState,
} from '../interfaces/web-request-state.interface';

export const NOT_ASKED_WEB_REQUEST: NotAskedState = {
  isNotAsked: true,
};

export const LOADING_WEB_REQUEST: LoadingState = {
  isLoading: true,
};
